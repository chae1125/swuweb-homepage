export type ApplyInfoCard = {
  label: string;
  value: string;
  hint?: string;
};

export type ApplyStep = {
  title: string;
  date: string;
  desc: string;
};

export type ApplyChecklistItem = {
  title: string;
  desc: string;
};
